schemaVersion: 1
id: pscale.dev/planetscale
name: PlanetScale
description: PlanetScale remote MCP server at
  https://mcp.pscale.dev/mcp/planetscale\n. Authenticated access to your
  Postgres and MySQL DBs. Setup guides for Claude Code, Codex, Cursor, and VS
  Code.
source:
  type: virtual
  origin:
    type: remote
    url: https://mcpservers.org/remote-mcp-servers/planetscale
capabilities:
  - id: planetscale
    kind: mcp
    spec:
      serverName: planetscale
      transport: remote
      sourceFormat: direct
      config:
        type: streamable-http
        url: https://mcp.pscale.dev/mcp/planetscale\n
